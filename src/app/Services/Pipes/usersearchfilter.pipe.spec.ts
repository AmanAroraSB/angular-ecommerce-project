import { UsersearchfilterPipe } from './usersearchfilter.pipe';

describe('UsersearchfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new UsersearchfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
